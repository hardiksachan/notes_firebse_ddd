import 'package:flutter/material.dart';

class NotesPlaceholderPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('Notes Overview'));
  }
}
